box: node:boron
build:
  steps:
    - script:
        name: Install global npm fluff
        code: npm install -g bower grunt-cli
    - npm-install 
    - npm-test
deploy:
  steps:
    - kwakayama/npm-publish@0.0.2
